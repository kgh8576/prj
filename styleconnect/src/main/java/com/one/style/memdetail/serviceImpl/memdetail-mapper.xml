<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.one.style.memdetail.mapper.MemdetailMapper">
	<!-- 핸드폰번호 변경 -->
	<update id="hpchange" parameterType="com.one.style.mem.vo.MemberVO">
		UPDATE MEMBER SET HP = #{hp} WHERE ID = #{id}
	</update>
	<!-- 비밀번호 변경 -->
	<update id="pwchange" parameterType="com.one.style.mem.vo.MemberVO">
		UPDATE MEMBER SET PW = #{pw} WHERE ID = #{id}
	</update>
	<!-- 상담내역 출력 -->
	<select id="conhisList">
		SELECT * FROM CONSULTING_HISTORY WHERE MEMID = #{memId}
	</select>
	<!-- 회원탈퇴 -->
	<delete id="memexit" parameterType="com.one.style.mem.vo.MemberVO">
		DELETE FROM MEMBER WHERE ID = #{id}
	</delete>
</mapper>