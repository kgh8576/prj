<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.one.style.files.mapper.FilesServiceMap">
	<select id="cergroupno" resultType="int">
		select cer_group_no_seq.nextval from dual;
	</select>
	<insert id="fileinsert" parameterType="com.one.style.files.vo.FilesVO">
		INSERT INTO FILES VALUES(#{,'cer')
	</insert>
</mapper>